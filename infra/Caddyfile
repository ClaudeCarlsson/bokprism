{
	email admin@bokprism.se
}

bokprism.se {
	encode zstd gzip

	reverse_proxy app:3000

	request_body {
		max_size 1MB
	}

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		-Server
	}
}

www.bokprism.se {
	redir https://bokprism.se{uri} permanent
}
